#Ingresar por teclado la cantidad de terminos a generar de la siguiente serie:

#El primer termino es el 1, cada termino se genera como el doble del termino anterior mas 5
#Mostrar la serie por pantalla e informar la suma de los terminos generados

cantidad = int(input("Ingrese la cantidad de terminos a generar: "))

termino = 1
suma = 0

for i in range(cantidad):
    print(termino)
    suma += termino
    termino = (termino * 2) + 5

print("Suma total:", suma)
