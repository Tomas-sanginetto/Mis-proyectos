# Leer dos numeros  A y B (enteros positivos).
# Calcular el producto A * B, pero por sumas
# Ej: 4* 3 = 4 + 4 + 4

num1 = int(input("Ingrese el primer numero:"))
num2 = int(input("Ingrese el numero por el cual quiere multiplicar al primero:"))
if num1 < 0 or num2 < 0:
    print("Tiene que ser un numero positivo, intentelo nuevamente")
    num1 = int(input("Ingrese el primer numero:"))
    num2 = int(input("Ingrese el numero por el cual quiere multiplicar al primero:"))

resultado = 0
for i in range(num2):
    resultado = resultado + num1
print("El resultado de la multiplicacion es:", resultado)

# PSEUDOCODIGO

# INICIO
# Leer dos numeros enteros positivos
# Calcular el producto del primer numero por el segundo, pero por sumas
# Mostrar el resultado
# FIN
